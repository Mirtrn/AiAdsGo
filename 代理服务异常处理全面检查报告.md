# 代理服务异常处理全面检查报告

**日期**: 2026-03-08
**检查范围**: 所有使用代理服务的任务执行器
**检查目的**: 确保所有执行器都能正确处理 IPRocket 等代理服务商的业务异常

---

## 一、检查概述

### 检查的执行器

系统中使用代理服务的主要执行器：

1. ✅ **url-swap-executor** - 换链接任务（已优化）
2. ⚠️ **click-farm-executor** - 补点击任务（需要优化）
3. ⚠️ **link-check-executor** - 链接检查任务（需要优化）
4. ⚠️ **scrape-executor** - 网页抓取任务（需要优化）
5. ✅ **offer-extraction-executor** - Offer提取任务（间接使用，通过 scrape-executor）

### 检查结果汇总

| 执行器 | 使用代理 | 错误处理 | IPRocket 特殊处理 | 状态 |
|--------|---------|---------|------------------|------|
| url-swap-executor | ✅ 是 | ✅ 完善 | ✅ **已优化** | ✅ 良好 |
| click-farm-executor | ✅ 是 | ⚠️ 基础 | ❌ 无 | ⚠️ 需优化 |
| link-check-executor | ✅ 是 | ⚠️ 基础 | ❌ 无 | ⚠️ 需优化 |
| scrape-executor | ✅ 是 | ⚠️ 基础 | ❌ 无 | ⚠️ 需优化 |

---

## 二、详细分析

### 1. url-swap-executor（换链接任务）✅

**文件**: `src/lib/queue/executors/url-swap-executor.ts`

**状态**: ✅ **已优化完成**

**错误处理逻辑**:
```typescript
// 优先检测 IPRocket 业务错误
if (
  rawMessage.includes('IPRocket') &&
  (rawMessage.includes('Business abnormality') ||
   rawMessage.includes('business error') ||
   rawMessage.includes('contact customer service'))
) {
  errorType = 'link_resolution'
  enhancedMessage =
    `🔴 IPRocket 代理服务商返回业务异常\n\n` +
    `可能原因：\n` +
    `1. 账户配额已用完 - 请检查 IPRocket 账户余额和流量\n` +
    `2. 账户被暂停或限制 - 请联系 IPRocket 客服确认账户状态\n` +
    `3. 触发风控限制 - 请降低请求频率或更换代理服务商\n` +
    `4. 服务商临时故障 - 请稍后重试\n\n` +
    `建议操作：\n` +
    `✓ 登录 IPRocket 控制台检查账户状态\n` +
    `✓ 考虑更换代理服务商（Oxylabs、Bright Data 等）\n` +
    `✓ 或暂时禁用部分任务，降低请求频率\n` +
    `✓ 修复后在任务详情页重新启用任务\n\n` +
    `原始错误: ${rawMessage}`
}
```

**优点**:
- ✅ 详细的错误提示
- ✅ 明确的解决建议
- ✅ 保留原始错误信息
- ✅ 错误分类清晰

---

### 2. click-farm-executor（补点击任务）⚠️

**文件**: `src/lib/queue/executors/click-farm-executor.ts`

**状态**: ⚠️ **需要优化**

**当前错误处理**:
```typescript
// 第 367-369 行
} catch (error: any) {
  console.error(`[ClickFarm] 动态获取代理失败: ${error.message}`)
}

// 第 384-386 行
} catch (e: any) {
  console.error(`[ClickFarm] 代理解析失败: ${maskProxyUrl(proxyUrl)} - ${e?.message || String(e)}`)
}

// 第 512-523 行
} catch (error: any) {
  console.error(`[ClickFarm] 执行器错误:`, error?.message || error);
  // 同步阶段失败，记为失败
  try {
    let scheduledHour: number | undefined;
    scheduledHour = getScheduledHour()
    await updateTaskStats(taskId, false, scheduledHour);
  } catch {
    // ignore
  }
  return { success: false, traffic: 0 };
}
```

**问题**:
- ❌ 没有检测 IPRocket 业务错误
- ❌ 错误提示不够详细
- ❌ 用户不知道如何解决问题
- ⚠️ 只记录到控制台，用户看不到

**影响**:
- 用户的补点击任务失败时，不知道是代理问题
- 无法快速定位和解决 IPRocket 账户问题
- 可能导致大量任务持续失败

**建议优化**:
需要在代理解析失败时，检测是否为 IPRocket 业务错误，并提供详细的错误提示。

---

### 3. link-check-executor（链接检查任务）⚠️

**文件**: `src/lib/queue/executors/link-check-executor.ts`

**状态**: ⚠️ **需要优化**

**当前错误处理**:
```typescript
// 第 79-84 行
} catch (error: any) {
  return {
    isValid: false,
    error: error.message || '链接解析失败'
  }
}

// 第 206-220 行
} catch (error: any) {
  const duration = Date.now() - startTime
  console.error(`❌ [LinkCheckExecutor] 链接检查失败: ${error.message}, 耗时=${duration}ms`)

  return {
    success: false,
    totalUsers: 0,
    totalLinks: 0,
    totalAlerts: 0,
    brokenLinks: 0,
    validLinks: 0,
    accountChecks: { totalAccounts: 0, problemAccounts: 0 },
    errorMessage: error.message,
    duration
  }
}
```

**问题**:
- ❌ 没有检测 IPRocket 业务错误
- ❌ 错误提示过于简单（只有 `error.message`）
- ❌ 用户不知道是代理问题还是链接问题
- ⚠️ 错误信息不够明确

**影响**:
- 链接检查失败时，用户可能误以为是链接本身的问题
- 实际上可能是代理服务异常
- 导致误判和错误的处理决策

**建议优化**:
需要区分链接问题和代理问题，特别是 IPRocket 业务错误。

---

### 4. scrape-executor（网页抓取任务）⚠️

**文件**: `src/lib/queue/executors/scrape-executor.ts`

**状态**: ⚠️ **需要优化**

**当前错误处理**:
```typescript
// 第 59-71 行
} catch (error: any) {
  console.error(`❌ [ScrapeExecutor] 抓取任务失败: Offer #${offerId}`, error.message)

  // 更新 offer 状态为失败
  try {
    const { updateOfferScrapeStatus } = await import('@/lib/offers')
    await updateOfferScrapeStatus(offerId, userId, 'failed', error.message)
  } catch (updateError) {
    console.error(`   更新状态失败:`, updateError)
  }

  throw error
}
```

**问题**:
- ❌ 没有检测 IPRocket 业务错误
- ❌ 错误提示直接使用 `error.message`，可能不够清晰
- ❌ 用户看到的错误信息可能难以理解
- ⚠️ 抓取失败的原因不明确（可能是代理、网络、网站问题等）

**影响**:
- Offer 抓取失败时，用户不知道具体原因
- 可能是代理问题，但用户以为是网站问题
- 导致错误的排查方向

**建议优化**:
需要在抓取失败时，分析错误类型，特别是代理相关的错误。

---

## 三、底层代理错误处理

### ProxyError 错误分类系统 ✅

**文件**: `src/lib/proxy/proxy-errors.ts`

系统已经有完善的代理错误分类：

```typescript
export class ProxyProviderBusinessError extends ProxyError {
  provider: string
  statusCode?: number

  constructor(provider: string, message: string, statusCode?: number) {
    super(message, false, 'PROVIDER_BUSINESS_ERROR')  // retryable = false
    this.name = 'ProxyProviderBusinessError'
    this.provider = provider
    this.statusCode = statusCode
  }
}
```

**特点**:
- ✅ 明确标记为 **不可重试** (`retryable = false`)
- ✅ 包含服务商名称
- ✅ 包含状态码
- ✅ 错误代码为 `PROVIDER_BUSINESS_ERROR`

### IPRocket Provider 错误检测 ✅

**文件**: `src/lib/proxy/providers/iprocket-provider.ts`

```typescript
function isBusinessAbnormalityError(code: number, message: string): boolean {
  const lowerMessage = String(message || '').toLowerCase()
  if (lowerMessage.includes('business abnormality')) return true
  if (lowerMessage.includes('contact customer service')) return true
  if (lowerMessage.includes('account abnormal')) return true
  if (lowerMessage.includes('risk control')) return true

  return code === 500 && lowerMessage.includes('abnormal')
}

function createIprocketApiError(code: number, message: string): ProxyError {
  const normalized = message || 'Unknown API error'
  if (isBusinessAbnormalityError(code, normalized)) {
    return new ProxyProviderBusinessError(
      'IPRocket',
      `IPRocket API business error: ${normalized}`,
      code
    )
  }
  return new ProxyHttpError(code, `IPRocket API error: ${normalized}`)
}
```

**特点**:
- ✅ 正确识别 IPRocket 业务错误
- ✅ 抛出 `ProxyProviderBusinessError`
- ✅ 避免无效重试

---

## 四、问题根源分析

### 为什么其他执行器没有详细的错误提示？

1. **错误传播链断裂**
   - IPRocket Provider 正确抛出 `ProxyProviderBusinessError`
   - 但错误在传播到执行器时，只保留了 `error.message`
   - 执行器没有检查错误类型或错误消息内容

2. **缺少统一的错误处理模式**
   - `url-swap-executor` 有详细的错误检测和提示
   - 其他执行器只是简单地记录 `error.message`
   - 没有统一的错误处理工具函数

3. **用户反馈机制不完善**
   - 大部分错误只记录到控制台
   - 用户需要查看数据库或日志才能看到错误
   - 缺少用户友好的错误展示

---

## 五、优化建议

### 优先级 P0（立即处理）

#### 1. 创建统一的代理错误处理工具函数

**新文件**: `src/lib/queue/executors/proxy-error-handler.ts`

```typescript
/**
 * 统一的代理错误处理工具
 * 用于所有执行器中的代理错误检测和提示
 */

export interface ProxyErrorAnalysis {
  isProxyError: boolean
  isIPRocketBusinessError: boolean
  enhancedMessage: string
  originalMessage: string
  suggestions: string[]
}

/**
 * 分析代理错误并生成详细的错误提示
 */
export function analyzeProxyError(error: any): ProxyErrorAnalysis {
  const rawMessage = error?.message || String(error)

  // 检测 IPRocket 业务错误
  const isIPRocketBusinessError =
    rawMessage.includes('IPRocket') &&
    (rawMessage.includes('Business abnormality') ||
     rawMessage.includes('business error') ||
     rawMessage.includes('contact customer service'))

  if (isIPRocketBusinessError) {
    return {
      isProxyError: true,
      isIPRocketBusinessError: true,
      enhancedMessage:
        `🔴 IPRocket 代理服务商返回业务异常\n\n` +
        `可能原因：\n` +
        `1. 账户配额已用完 - 请检查 IPRocket 账户余额和流量\n` +
        `2. 账户被暂停或限制 - 请联系 IPRocket 客服确认账户状态\n` +
        `3. 触发风控限制 - 请降低请求频率或更换代理服务商\n` +
        `4. 服务商临时故障 - 请稍后重试\n\n` +
        `建议操作：\n` +
        `✓ 登录 IPRocket 控制台检查账户状态\n` +
        `✓ 考虑更换代理服务商（Oxylabs、Bright Data 等）\n` +
        `✓ 或暂时禁用部分任务，降低请求频率\n\n` +
        `原始错误: ${rawMessage}`,
      originalMessage: rawMessage,
      suggestions: [
        '检查 IPRocket 账户状态',
        '考虑更换代理服务商',
        '降低请求频率'
      ]
    }
  }

  // 检测其他代理错误
  const isProxyError =
    rawMessage.includes('proxy') ||
    rawMessage.includes('代理') ||
    rawMessage.includes('ProxyError') ||
    rawMessage.includes('获取代理IP失败')

  if (isProxyError) {
    return {
      isProxyError: true,
      isIPRocketBusinessError: false,
      enhancedMessage: `代理服务错误: ${rawMessage}`,
      originalMessage: rawMessage,
      suggestions: ['检查代理配置', '确认代理服务可用']
    }
  }

  // 非代理错误
  return {
    isProxyError: false,
    isIPRocketBusinessError: false,
    enhancedMessage: rawMessage,
    originalMessage: rawMessage,
    suggestions: []
  }
}
```

#### 2. 优化 click-farm-executor

在 `src/lib/queue/executors/click-farm-executor.ts` 中：

```typescript
// 在文件顶部导入
import { analyzeProxyError } from './proxy-error-handler'

// 修改第 384-391 行的错误处理
} catch (e: any) {
  const errorAnalysis = analyzeProxyError(e)
  console.error(`[ClickFarm] 代理解析失败: ${maskProxyUrl(proxyUrl)}`)
  console.error(`错误详情: ${errorAnalysis.enhancedMessage}`)

  // 如果是 IPRocket 业务错误，记录到任务状态中
  if (errorAnalysis.isIPRocketBusinessError) {
    // TODO: 将错误信息保存到 click_farm_tasks 表，让用户可以看到
  }
}
```

#### 3. 优化 link-check-executor

在 `src/lib/queue/executors/link-check-executor.ts` 中：

```typescript
// 在文件顶部导入
import { analyzeProxyError } from './proxy-error-handler'

// 修改第 79-84 行的错误处理
} catch (error: any) {
  const errorAnalysis = analyzeProxyError(error)
  return {
    isValid: false,
    error: errorAnalysis.isProxyError
      ? errorAnalysis.enhancedMessage
      : (error.message || '链接解析失败')
  }
}
```

#### 4. 优化 scrape-executor

在 `src/lib/queue/executors/scrape-executor.ts` 中：

```typescript
// 在文件顶部导入
import { analyzeProxyError } from './proxy-error-handler'

// 修改第 59-71 行的错误处理
} catch (error: any) {
  const errorAnalysis = analyzeProxyError(error)
  const errorMessage = errorAnalysis.isProxyError
    ? errorAnalysis.enhancedMessage
    : error.message

  console.error(`❌ [ScrapeExecutor] 抓取任务失败: Offer #${offerId}`, errorMessage)

  // 更新 offer 状态为失败
  try {
    const { updateOfferScrapeStatus } = await import('@/lib/offers')
    await updateOfferScrapeStatus(offerId, userId, 'failed', errorMessage)
  } catch (updateError) {
    console.error(`   更新状态失败:`, updateError)
  }

  throw error
}
```

### 优先级 P1（本周内）

1. **添加用户通知机制**
   - 当检测到 IPRocket 业务错误时，发送站内信/邮件通知
   - 提供快速解决链接

2. **代理服务商健康监控**
   - 统计各服务商的成功率
   - 自动告警异常情况

3. **错误统计和分析**
   - 按错误类型统计
   - 生成错误趋势报告

### 优先级 P2（长期优化）

1. **多代理服务商自动切换**
   - 当主服务商失败时，自动尝试备用服务商
   - 实现故障转移

2. **代理质量评分系统**
   - 根据成功率、响应时间等指标评分
   - 优先使用高质量代理

---

## 六、总结

### 当前状态

- ✅ **url-swap-executor** 已经有完善的 IPRocket 错误处理
- ⚠️ **其他 3 个执行器** 需要添加类似的错误处理
- ✅ 底层的错误分类系统已经完善
- ❌ 缺少统一的错误处理工具函数

### 风险评估

| 执行器 | 使用频率 | 影响范围 | 风险等级 |
|--------|---------|---------|---------|
| click-farm-executor | 高 | 补点击功能 | 🔴 高 |
| link-check-executor | 中 | 链接检查 | 🟡 中 |
| scrape-executor | 高 | Offer抓取 | 🔴 高 |

### 建议行动

1. **立即**: 创建统一的代理错误处理工具函数
2. **本周**: 优化 click-farm-executor 和 scrape-executor
3. **下周**: 优化 link-check-executor
4. **持续**: 监控代理服务商健康度，及时发现问题

---

**报告生成时间**: 2026-03-08
**报告生成人**: Claude (AI 助手)
